name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always
  # Fail the build on any warning rather than letting drift accumulate — the
  # tree is warning-free as of UC-09, so a new warning is a new regression.
  RUSTFLAGS: -D warnings

jobs:
  check:
    name: fmt, clippy, test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # ffmpeg-next's underlying ffmpeg-sys-next crate needs the ffmpeg C
      # dev libraries at build time (issue #44 video slice) — the first
      # system dependency this workspace has needed.
      - name: Install ffmpeg dev libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libavformat-dev libavcodec-dev libavutil-dev \
            libavfilter-dev libavdevice-dev libswscale-dev \
            libswresample-dev pkg-config clang

      - name: Install the toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry and build output
        uses: Swatinem/rust-cache@v2

      # Formatting first: it is the cheapest gate and its failures are the
      # least interesting to read past.
      - name: Check formatting
        run: cargo fmt --all -- --check

      # `--all-targets` so the lint pass covers tests and benches, not just
      # the libraries — most of the workspace's code lives in its tests.
      - name: Lint
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Test
        run: cargo test --workspace

  # NFR-02 (indexing throughput) and NFR-01's read latency under load are
  # measurements, not assertions, so they are deliberately not part of the gate
  # above: a shared runner's throughput says more about the runner than about
  # the commit. This job exists so the harness cannot rot unnoticed and so the
  # figures are recorded run over run — read them in the log.
  #
  # It runs without ALEXANDRIA_NFR_STRICT, so the assertions are the loose
  # floors (see the Testing Specification Document §9): low enough that runner
  # noise cannot trip them, tight enough to catch a re-serialized walk or
  # blocking I/O back on the async runtime. The requirement's actual 500
  # files/sec target is verified on a developer machine, which is what NFR-02
  # scopes it to.
  performance:
    name: performance (non-gating)
    runs-on: ubuntu-latest
    # Only on main: on a pull request this would double CI time to report a
    # number nobody reviews.
    if: github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: Install ffmpeg dev libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libavformat-dev libavcodec-dev libavutil-dev \
            libavfilter-dev libavdevice-dev libswscale-dev \
            libswresample-dev pkg-config clang

      - name: Install the toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry and build output
        uses: Swatinem/rust-cache@v2

      - name: Measure indexing throughput and read latency
        run: |
          cargo test -p alexandria-core --test throughput \
            -- --ignored --nocapture --test-threads=1
